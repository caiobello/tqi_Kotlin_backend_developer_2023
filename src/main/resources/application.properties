# Configurações do banco de dados H2 (para testes e desenvolvimento)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Configurações do banco de dados PostgreSQL (para produção)
#spring.datasource.url=jdbc:postgresql://localhost:5432/meu_banco
#spring.datasource.username=meu_usuario
#spring.datasource.password=senha_do_banco
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configurações do Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

## Swagger-ui
springdoc.api-docs.path=/tqi_Kotlin_backend_developer_2023

# Configurando ThymeLeaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Security

# Configurações do servidor embutido Tomcat
server.port=8080

# Configurações de segurança
# Configurar autenticação e autorização conforme necessário
# Exemplo:
# spring.security.user.name=admin
# spring.security.user.password=admin

# Configurações do logging
logging.level.org.springframework=INFO
logging.level.com.atendimento.market=DEBUG



